# Prod compliance + CRM profile - merge after .env.example
#
# Usage:
#   cp .env.example .env
#   cat config/env/.env.prod.example >> .env
#   # set GEMINI_API_KEY, PARSER_CRM_WEBHOOK_SECRET, TELEGRAM_API_*, PARSER_PROXY_LIST
#   go run ./cmd/parser config check
#
# Covers: geo gate, defer warm path, CRM webhook after analysis, global discovery.

PARSER_SEED_PROFILE=prod
PARSER_SOURCE=forum,supply,lander,reddit,warrior,serp,reviews,tgweb
PARSER_BG_TELEGRAM=true
PARSER_POLL_SEC=120

# Geo + Gemini (required for CRM webhook)
GEMINI_API_KEY=
PARSER_GEO_CLASSIFY=true
PARSER_GEMINI_DEFER=true
PARSER_GEMINI_SYNC_GEO=false
PARSER_ICP_CLASSIFY=true
PARSER_ICP_CLASSIFY_TGWEB=true
PARSER_TGWEB_PRESCAN_MODE=strict
PARSER_ENRICH_RDAP=true
GEO_BLOCK_COUNTRIES=RU,BY

# CRM handoff (applyComplianceDefaults also sets these when PARSER_CRM_WEBHOOK=true)
PARSER_LEAD_STATUS_ENABLED=true
PARSER_CRM_WEBHOOK=true
PARSER_CRM_WEBHOOK_URL=http://127.0.0.1:8080/v1/leads
PARSER_CRM_WEBHOOK_SECRET=
PARSER_CRM_WEBHOOK_AFTER_ANALYSIS=true
PARSER_CRM_WEBHOOK_HEAT_MIN=hot

# Entity heat (cross-source graph)
PARSER_ENTITY_HEAT_ENABLED=true
KEYWORDS_LOCALE=es,pt
GEMINI_DISCOVER_DIFF_EVERY=5
GEMINI_DISCOVER_DIFF_DIR=data/suggestions
REDDIT_SUBREDDITS=affiliatemarketing,media_buying,adops,PPC,juststart
TELEGRAM_GEO_HEURISTIC=true

SUPPLY_SEED_PATH=data/seeds/domains.prod.csv
LANDER_SEED_PATH=data/seeds/lander_urls.prod.csv
FORUM_SEED_PATH=data/seeds/forum_threads.live.csv
TELEGRAM_DOMAINS_PATH=data/runtime/discovered_telegram_domains.json

MONGO_URI=mongodb://127.0.0.1:27017
PARSER_EXPORT_JSON_HOST=data/export/leads.jsonl
PARSER_METRICS_ADDR=:9465

# PARSER_PROXY_LIST=http://user:pass@proxy:823
