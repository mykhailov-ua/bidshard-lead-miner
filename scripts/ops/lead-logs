#!/usr/bin/env bash
# Stream VPS docker logs.
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
# shellcheck disable=SC1091
source "$ROOT/scripts/lib/vps_ssh.sh"
# shellcheck disable=SC1091
source "$ROOT/scripts/lib/lead_logs.sh"
lead_logs_from_vps "$ROOT" -f --tail 100 "$@"
